# PostgreSQL Client Authentication Configuration File
# Generated by Ansible for {{ db_name }}

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     peer

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Additional allowed client CIDRs (set via postgresql_client_cidrs)
{% for cidr in postgresql_client_cidrs | default([]) %}
{% if cidr not in ['127.0.0.1/32', '::1/128'] %}
host    all             all             {{ cidr }}            scram-sha-256
{% endif %}
{% endfor %}

# Replication connections
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256

